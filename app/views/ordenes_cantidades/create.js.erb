$('#errores').html('<%= j (render("errors", :formats => [:html])) %>');
<% unless @orden_cantidad.errors.any? %>
	$('#cantidades').html("<%= j (render @ordenes_cantidades) %>");
	$('#total').html("<%= j ("#{number_to_currency(@orden.total)}") %>");
	$('#cantidad-form').slideUp(350);
<% end %>
